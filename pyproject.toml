[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "zzx-terminal"
version = "7.0.0"
description = "Pure Real ZZX Terminal"
dependencies = ["lupa>=1.14.1"]

[tool.setuptools]
py-modules = ["terminal", "uvan7"]
